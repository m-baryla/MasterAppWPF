<UserControl x:Class="App_1.PluginBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App_1"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Grid.Column="0"
                Content="Regular" 
                Padding="10" 
                Margin="10"
                Command="{Binding RegularCommand}"/>

        <Button Grid.Row="0" Grid.Column="1"
                Content="Trigonometry" 
                Padding="10" 
                Margin="10"
                Command="{Binding TrigonometryCommand}"/>


        <ContentControl  Grid.Row="1" Grid.Column="1" Name ="CustomContentControl"/>

        <!--<ContentControl  Grid.Row="1" Grid.Column="1" Content="{Binding CustomContentControl}"/>-->

</Grid>
</UserControl>
